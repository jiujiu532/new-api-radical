# 🎉 v1.2.4 - 定时封禁增强 + Claude 签名修复 + 模型健康度优化

## ✨ 新增功能

### ⏱️ 定时封禁分钟级支持
- **分钟级精度**：封禁时长支持 1分钟、5分钟、30分钟等短时封禁
- **自定义分钟数**：管理员可输入任意分钟数（1分钟 ~ 1年）
- **到期自动解封**：登录、API调用、OAuth登录时自动检测并解封
- **小黑屋实时倒计时**：显示"N天N小时"/"N分N秒"/"N秒"等精确剩余时间
- **全链路覆盖**：Session、Access Token、API Token 三条认证路径均支持

### 🔧 Claude Thinking 签名注入
- **自动修复**：上游未返回 `signature` 字段时，自动注入空签名占位
- **流式+非流式**：同时覆盖 SSE 流式和 JSON 非流式两种响应
- **兼容增强**：解决 Cherry Studio 等客户端 Zod 校验 `Invalid JSON response` 报错
- **透明处理**：上游已有签名时不干预，零副作用

### 📊 模型健康度页面优化（融合上游）
- **骨架屏加载**：首次加载显示骨架动画，替代空白等待
- **Token 统计**：每个模型显示 Token 消耗量，tooltip 展示详情
- **紧凑数字格式**：大数字自动转换为 K/M/G 等单位
- **排序优化**：模型列表按 Token 消耗量排序

## 🔧 代码优化

- 中间件 `auth.go` 移除 `goto` 语句，消除冗余 Redis 查询
- 抽取 `CheckAndAutoUnbanById` 函数，统一定时封禁检查逻辑
- 8 个 OAuth 控制器统一封禁检查模式
- ManageUser 使用 `map[string]interface{}` 更新，兼顾 GORM 零值字段

## 📥 下载

| 平台 | 文件 |
|------|------|
| Linux (amd64) | `new-api-linux-amd64` |
| Windows (amd64) | `new-api-windows-amd64.exe` |

## 🔄 升级说明

直接替换二进制文件或更新 Docker 镜像即可，无需数据库迁移。

> [!NOTE]
> `banned_at` 和 `ban_duration` 字段会在首次启动时由 GORM AutoMigrate 自动添加。

---

**Full Changelog**: https://github.com/jiujiu532/new-api-radical/compare/v1.2.3...v1.2.4
